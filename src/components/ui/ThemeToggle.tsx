'use client';

// Dark/light mode toggle option is permanently removed.
// The website is strictly and exclusively in Dark Mode on all systems.
export function ThemeToggle({ className = '' }: { className?: string }) {
  return null;
}
